zstyle ':completion:*:corrections' format ' -- %d (errors: %e) --%f'
zstyle ':completion:*:descriptions' format ' -- %d --%f'
zstyle ':completion:*:messages' format ' -- %d --%f'
zstyle ':completion:*:warnings' format ' -- no matches found --%f'
zstyle ':completion:*' format ' -- %d --%f'
